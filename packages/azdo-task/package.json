{
  "name": "@extension-tasks/azdo-task",
  "version": "6.0.0-alpha.1",
  "description": "Azure Pipelines task adapter for extension tasks",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "test": "jest",
    "bundle": "rollup -c ../../rollup.config.ts --configPlugin typescript"
  },
  "dependencies": {
    "@extension-tasks/core": "*",
    "azure-pipelines-task-lib": "^4.19.2",
    "azure-pipelines-tool-lib": "^2.0.14",
    "azure-arm-rest": "^2.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.24"
  }
}
